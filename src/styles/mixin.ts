import { blue, greyGrey, white } from './varibles';

export const button = `
    padding: 8px 22px;
    background-color: ${blue};
    color: ${white};
    font-size: 15px;
    font-weight: 500;
    line-height: 26px;
    letter-spacing: 0.3px;
    text-transform: uppercase;
    border-radius: 4px;
    transition: background-color 0.3s ease-in-out;
`;

export const formWrap = `
    padding-top: 32px;
    padding-bottom: 27px;
    border-bottom: 1px solid ${greyGrey};
`;
